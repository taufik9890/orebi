import React from 'react'

const SmallerText = ({title}) => {
  return (
    <div>
      <p className='text-base text-secondaryGray font-dm font-normal'>{title}</p>
    </div>
  )
}

export default SmallerText
